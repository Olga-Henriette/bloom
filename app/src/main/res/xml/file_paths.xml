<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Répertoire de stockage interne de l'app -->
    <files-path
        name="bloom_images"
        path="." />

    <!-- Cache de l'app -->
    <cache-path
        name="bloom_cache"
        path="." />
</paths>